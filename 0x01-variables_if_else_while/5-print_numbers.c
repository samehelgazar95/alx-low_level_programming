#include <stdio.h>
/**
 * main - entry point
 *
 * description: print digit numbers 0..1
 *
 * Return: Always (0) Success
*/

int main(void)
{
	int i = 0;

	while (i < 11)
	{
		printf("%d", i);
		i++;
	}
	return (0);
}
