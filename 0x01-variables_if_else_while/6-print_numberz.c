#include <stdio.h>
/**
 * main - entry point
 *
 * descrition: Print from 0..1 digit just using putchar
 *
 * Return: Always (0) Success
*/

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		putchar('i' - 0);
		i++;
	}
	return (0);
}
